save-position-on-quit=yes

# Use hardware decoding where available, fall back to software if not supported.
hwdec=auto-safe

# Cache ahead for smoother seeking in large files.
cache=yes
demuxer-max-bytes=500MiB
