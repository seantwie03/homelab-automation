// Lock after 15 minutes, suspend after 15 minutes, lock again on resume.
spawn-at-startup "swayidle" "-w" "timeout" "900" "swaylock -f" "timeout" "900" "systemctl suspend" "after-resume" "swaylock -f"
