# AI

Install AI development tools for the user specified in the `user` variable.

The role installs Gemini CLI, Codex, and Claude Code. Antigravity configuration
is included but disabled in `tasks/main.yml`.

