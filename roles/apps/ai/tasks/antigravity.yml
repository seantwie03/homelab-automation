---
- name: antigravity repository is configured
  ansible.builtin.copy:
    src: antigravity.repo
    dest: /etc/yum.repos.d/antigravity.repo
    owner: root
    group: root
    mode: 0644

- name: antigravity is installed
  ansible.builtin.package:
    name: antigravity
    state: present

