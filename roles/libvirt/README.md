# Libvirt (KVM/QEMU)

Install Virtualization Tools included in `@virtualization` group.

Add user specified in `user` variable to libvirt group.

For storage pool setup version `1.4.0+` of the [community.libvirt](https://galaxy.ansible.com/ui/repo/published/community/libvirt/) collection is required.

