# .NET

Installs Fedora's .NET 10 SDK package.

The SDK provides the runtime required by the Roslyn language server. Mason
installs and updates `roslyn-language-server` for Neovim and Emacs, but Mason
does not install the .NET runtime itself.
