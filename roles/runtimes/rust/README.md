# Rust

Install Cargo, Clippy, and the Rust standard-library source. The Fedora Cargo
package provides the Rust toolchain, Clippy provides additional linting through
`cargo clippy`, and `rust-src` lets language servers such as Rust Analyzer
inspect standard-library types and macros.
