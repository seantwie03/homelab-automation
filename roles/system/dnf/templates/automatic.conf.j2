[commands]
upgrade_type = default
apply_updates = yes
reboot = {{ dnf_automatic_reboot }}
reboot_command = "systemctl reboot"

[emitters]
emit_via = stdio, command

[command]
command_format = /usr/local/bin/dnf-automatic-notify.sh
stdin_format = {body}

[base]
debuglevel = 1
