# RPM Fusion

Enable [RPM Fusion](https://rpmfusion.org/RPM%20Fusion) repositories.

The RPM Fusion release RPMs own the repository files, so this role keeps the upstream remote release RPM installation flow.
Networked key imports and release RPM installs use retries to reduce transient failures.
