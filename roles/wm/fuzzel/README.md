# Fuzzel

Installs Fuzzel as an application launcher for Wayland window-manager sessions.

This role installs `fuzzel` and `rofimoji`, then symlinks `dotfiles/fuzzel` to
`~/.config/fuzzel`.

Window-manager roles can depend on this role when they need the launcher and
emoji picker available.

